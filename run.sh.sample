#!/usr/bin/env bash

current_directory=$(pwd)

# Store the first argument in a variable
operation_type="$1"

# Remove the first argument from the list
shift

modal run <directory>/main.py --command "${operation_type}" --directory "${current_directory}" "$@"